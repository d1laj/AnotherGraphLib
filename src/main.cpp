#include "SmallBipSignedGraphDelta3.hpp"
#include <iostream>
#include <bitset>

using namespace std;
int main(){
  u8 value1 = 3;
  u8 value2 = 65;
/*
  VertexDataView v1(value1);
  VertexDataView v2(value2);

  //int a = 3;
  for (int a = 0; a < value2; a++){
    cout << "bli\n";
  }

  cout << (int)v1.deg.val() << " " <<  bitset<8>(value1) << "\n";
  cout << (int)v2.deg.val() << " " <<  bitset<8>(value2) << "\n";
  v1.deg++;
  v2.deg++;
  cout << (int)v1.deg.val() << " " << bitset<8>(value1) << "\n";
  cout << (int)v2.deg.val() << " " << bitset<8>(value2) << "\n";

  cout << (int)v1.str.val() << " " <<  bitset<8>(value1) << " " <<  bitset<8>(v1.str.val()) <<"\n";
  cout << (int)v2.str.val() << " " <<  bitset<8>(value2) << "\n";
  v1.str++;
  v2.str++;
  cout << (int)v1.str.val() << " " <<  bitset<8>(value1) << "\n";
  cout << (int)v2.str.val() << " " <<  bitset<8>(value2) << "\n";
  */
  return 0;
}
